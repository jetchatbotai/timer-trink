workflows:
  android-release:
    name: Android Release
    max_build_duration: 60

    environment:
      node: 20
      java: 21

    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true

    scripts:
      - name: Install dependencies
        script: |
          npm install

      - name: Check web files
        script: |
          echo "Checking web assets..."
          ls -la
          ls -la www
          test -f www/index.html
          test -f www/app.js

      - name: Backup raw sounds if present
        script: |
          mkdir -p /tmp/timer_raw_backup
          if [ -d android/app/src/main/res/raw ]; then
            cp -R android/app/src/main/res/raw/. /tmp/timer_raw_backup/ || true
          fi
          ls -la /tmp/timer_raw_backup || true

      - name: Ensure Android platform exists
        script: |
          if [ ! -f android/gradlew ]; then
            echo "gradlew missing -> recreating android platform"
            rm -rf android
            npx cap add android
          fi

      - name: Restore raw sounds after android recreation
        script: |
          mkdir -p android/app/src/main/res/raw
          if [ -d /tmp/timer_raw_backup ]; then
            cp -R /tmp/timer_raw_backup/. android/app/src/main/res/raw/ || true
          fi
          ls -la android/app/src/main/res/raw || true

      - name: Sync Capacitor Android
        script: |
          npx cap sync android

      - name: Verify Android files
        script: |
          ls -la android
          test -f android/gradlew
          test -f android/app/src/main/AndroidManifest.xml

      - name: Build Android release APK
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Build Android release AAB
        script: |
          cd android
          chmod +x gradlew
          ./gradlew bundleRelease

    artifacts:
      - android/app/build/outputs/**/*.apk
      - android/app/build/outputs/**/*.aab

    cache:
      cache_paths:
        - ~/.npm
        - ~/.gradle/caches
        - ~/.gradle/wrapper

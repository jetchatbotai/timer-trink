{
  "name": "timer-trink",
  "version": "1.0.0",
  "private": true,
  "description": "Timer Trink Capacitor Android wrapper",
  "scripts": {
    "build": "echo \"Static web app - no build step required\"",
    "cap:sync": "npx cap sync android",
    "cap:add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
